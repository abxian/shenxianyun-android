<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ThemeOverlay" />

    <style name="ThemeOverlay.ControlColorError">
        <item name="colorControlNormal">@color/color_error</item>
        <item name="android:textColor">@color/color_error</item>
    </style>

    <style name="ThemeOverlay.SxyIcon">
        <item name="colorControlNormal">@color/sxy_icon_tint</item>
        <item name="android:colorControlNormal">@color/sxy_icon_tint</item>
    </style>
</resources>
