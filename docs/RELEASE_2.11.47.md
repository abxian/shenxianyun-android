# 神仙云 Android v2.11.47

- 应用进程每次启动后自动刷新一次当前受管订阅，减少用户打开软件后继续使用旧节点的情况。
- 自动刷新失败时保留原配置，不覆盖当前可用订阅，也不重复计算提取次数。
- 代理连接期间每约 60 秒同步一次提取码状态和到期时间。
- 代理未连接但首页处于前台时，也会每约 60 秒轻量同步到期时间，网站续费后无需重启客户端。
- 保持用户手动选择的普通配置；后台刷新不会擅自切换当前配置。
- 继续兼容旧版订阅导入协议；vpn-web 全保护模式保持关闭。
