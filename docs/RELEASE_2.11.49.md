# 神仙云 Android v2.11.49

- 流量统计改为读取核心原始累计计数，避免界面采样遗漏造成计费不准确。
- 累计计数器持久化并按序号幂等上报，断网、重启和服务繁忙后可以安全续报。
- 上报改为五分钟基础间隔并加入随机抖动，减少大量客户端同时访问服务器。
- 上报失败时指数退避到最长三十分钟，避免网络或服务器恢复时形成请求惊群。
- 保持单请求在途，防止慢请求与下一轮定时任务重叠。
- 继续保留订阅自动更新的重试、断点续传和原子发布保护。
